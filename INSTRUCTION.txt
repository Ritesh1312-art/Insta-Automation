REAL META + UPI DEPLOYMENT CHECKLIST

1. Copy .env.example into Vercel / host env. Generate unique AUTH_SECRET, ENCRYPTION_KEY, CRON_SECRET, SETUP_TOKEN, META_VERIFY_TOKEN.
2. Set APP_URL and META_REDIRECT_URI to the HTTPS production origin.
3. Set UPI_ID and UPI_PAYEE_NAME. Checkout QR auto-generates from those values (exact plan amount). Optional: override in Settings as ADMIN.
4. Deploy. prisma generate + prisma db push run during npm run build.
5. Open /setup once, create the first ADMIN, then lock SETUP_TOKEN away.
6. Meta Developers: OAuth redirect /api/auth/meta/callback, webhook /api/webhooks/meta, subscribe comments + messages + messaging_postbacks.
7. Sign in, connect a professional Instagram account, sync posts, create a resource, enable follow-gate.
8. Confirm Vercel Cron (or an external 5-minute ping) hits /api/jobs/process-webhooks with Bearer CRON_SECRET.
9. Customers pay UPI → /verify → PENDING_REVIEW. Approve in Dashboard → UPI reviews only after the UTR hits your bank app.
10. Never market unlimited DMs. Premium Pro Plus is a 5,000 / month cap.
